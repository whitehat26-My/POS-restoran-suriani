{
  "name": "@suriani/pos-android",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The cashier tablet: the POS web app in a Capacitor shell, plus the two things a browser can never do — raw sockets to a printer, and listening on one so phones can order when the internet is down.",
  "scripts": {
    "web": "pnpm --filter @suriani/pos exec vite build --base ./ --outDir ../pos-android/www --emptyOutDir",
    "sync": "pnpm --filter @suriani/pos-android web && cap sync android && pnpm --filter @suriani/pos-android menu",
    "apk": "pnpm --filter @suriani/pos-android sync && cd android && ./gradlew assembleDebug",
    "menu": "pnpm --filter @suriani/menu exec vite build --outDir ../pos-android/android/app/src/main/assets/menu --emptyOutDir"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
