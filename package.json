{
  "name": "suriani-pos",
  "version": "0.0.0",
  "private": true,
  "description": "Multi-tenant QR-ordering POS for Malaysian restaurants",
  "scripts": {
    "dev": "pnpm --filter @suriani/api dev",
    "test": "pnpm --filter @suriani/api test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "seed": "pnpm --filter @suriani/api seed",
    "deploy": "pnpm --filter @suriani/api deploy",
    "build:web": "pnpm --filter @suriani/menu build && pnpm --filter @suriani/pos build && node scripts/assemble-assets.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.0"
}
